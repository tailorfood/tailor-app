import React, { Component } from 'react'
import {
    View,
    ScrollView,
    Image,
    Text,
    TouchableOpacity
} from 'react-native'

export default class Restaurant extends Component {
    render() {
        return (
            <View>
                <Text>header</Text>
                <ScrollView/>
            </View>
        )
    }
}